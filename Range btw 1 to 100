#include <stdio.h>

int main() {
int number;
printf("enter the number\n");
scanf("%d",&number);
if(number>=1 && number<=100)
printf("the number is within the range 1 to 100\n");
else 
printf("the number is not within the range 1 to 100\n");
    return 0;
}
