#include <stdio.h>

int main() {
    int n;
    printf("enter the number");
    scanf("%d",&n);
    for(int i=1 ;i<=19;i++){
        printf("number is %d %d %d\n",i,i*i,i*i*i);
    }

    return 0;
}
