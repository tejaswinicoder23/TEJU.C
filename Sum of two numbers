#include <stdio.h>

int main() { //to calculate sum of two numbers int a,b,s; printf("Enter the values of a and b\n"); scanf("%d%d",&a,&b); s=a+b; printf("the sum of a and b is %d\n",s); return 0; } #include <stdio.h>

int main() { // Write C code to calculate simple intrest float p,t,r,si; printf("enter the principle,time,rate of interest \n"); scanf("%f%f%f",&p,&t,&r); si=(ptr)/100; printf("the simple interest:",si);

return 0;
}
